import { Box } from "@theme-ui/components";
function Divider() {
  return (
    <Box
      sx={{
        background: "white",
        opacity: "20%",
        height: "0.2rem",
        margin: "auto",
        marginTop: "4rem",
        width: "70%",
        marginBottom: "1rem",
      }}
      className="divider"
    />
  );
}

export default Divider;
